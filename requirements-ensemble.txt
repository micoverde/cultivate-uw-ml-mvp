# Ensemble ML Model Requirements
# Issue #192: Containerize Ensemble ML Models
# These are the specific ML libraries needed for the 7-model ensemble

# Core ML Libraries (already in requirements.txt but specified for clarity)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Ensemble-Specific ML Libraries
xgboost>=1.7.0          # XGBoost classifier
lightgbm>=4.0.0         # LightGBM classifier

# Azure Integration
azure-storage-blob>=12.19.0  # For model storage/retrieval
azure-identity>=1.15.0       # For managed identity auth

# Caching and Performance
redis>=5.0.0            # For prediction caching
hiredis>=2.2.0          # C parser for redis (performance)

# Model Serialization
cloudpickle>=3.0.0      # Better serialization for complex models
dill>=0.3.7             # Alternative serialization

# Monitoring and Metrics
prometheus-client>=0.19.0    # Metrics export
psutil>=5.9.0               # System resource monitoring

# API Requirements (if not already included)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Optional but recommended for production
python-multipart>=0.0.6      # For file uploads
httpx>=0.25.0               # For async HTTP calls
tenacity>=8.2.0             # For retry logic

# Development/Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
locust>=2.17.0          # For load testing