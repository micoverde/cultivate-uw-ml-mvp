name: Azure Static Web Apps CI/CD

on:
  push:
    branches: [ main ]
    paths: [ 'demo/**', '.github/workflows/azure-static-web-apps.yml' ]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [ main ]
    paths: [ 'demo/**' ]

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'demo/package-lock.json'

      - name: Install and Build
        run: |
          cd demo
          npm ci
        env:
          NODE_ENV: production

      - name: Build with Production Environment
        run: |
          cd demo
          npm run build
        env:
          NODE_ENV: production
          VITE_API_BASE_URL: https://cultivate-ml-api-pag.westus2.azurecontainer.io:8000
          VITE_ENVIRONMENT: production
          VITE_ENABLE_WEBSOCKETS: true
          VITE_ENABLE_ANALYTICS: true
          VITE_DEMO_MODE: enhanced

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "demo"
          output_location: "dist"
          # api_location: "api"  # Removed: API deployed separately via container deployment

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"